package cn.com.easy.test;


public class TarTestCase {
	
	public static void main(String[] args) throws Exception {
	
//		String inFilename = "E:\\SSHstudy\\AutoDeplojyTemplate\\target\\AutoDeplojyTemplate-0.0.1.rar";
		// TarArchiveInputStream input = new TarArchiveInputStream(new
		// FileInputStream(inFilename));
		// TarArchiveEntry entry;
		// while ((entry = input.getNextTarEntry()) != null) {
		// System.out.println(entry.getName());
		// }
		// ZipArchiveInputStream input = new ZipArchiveInputStream(new
		// FileInputStream(inFilename));
		// ZipArchiveEntry entry = input.getNextZipEntry();
		// while ((entry = input.getNextZipEntry()) != null) {
		// System.out.println(entry.getName());
		// }
//		JarArchiveInputStream input = new JarArchiveInputStream(new FileInputStream(inFilename));
//		JarArchiveEntry entry = input.getNextJarEntry();
//		while ((entry = input.getNextJarEntry()) != null) {
//			System.out.println(entry.getName());
//		}
		
	}
}
